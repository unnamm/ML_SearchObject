<UserControl x:Class="UI.View.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.View"
             xmlns:component="clr-namespace:UI.ComponentView"
             xmlns:converter="clr-namespace:UI.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBlock.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="LoadImage" Width="200" Command="{Binding LoadImageCommand}"/>
                    <Button Content="RunPredict" Width="200" Command="{Binding RunPredictCommand}"/>
                    <TextBlock Text="{Binding LoadedImage, Converter={converter:PathToFileName}}"/>
                </StackPanel>
                <component:ImageViewer ImageSource="{Binding ImageData}" Background="Black" Cursor="Hand" Stretch="UniformToFill"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{Binding LogInstance.LogList}" Background="Transparent"/>
    </Grid>
</UserControl>
