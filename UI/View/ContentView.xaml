<UserControl x:Class="UI.View.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.View"
             xmlns:component="clr-namespace:UI.ComponentView"
             xmlns:converter="clr-namespace:UI.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBlock.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="LoadModel" Width="150" Command="{Binding LoadModelCommand}"/>
                    <Button Content="LoadImage" Width="150" Command="{Binding LoadImageCommand}"/>
                    <Button Content="RunPredict" Width="150" Command="{Binding RunPredictCommand}"/>
                    <TextBlock Text="{Binding LoadedImage, Converter={converter:PathToFileName}}"/>
                </StackPanel>
                <component:ImageViewer ImageSource="{Binding ImageData}" Background="Black" Cursor="Hand" Stretch="UniformToFill"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <GroupBox Header="Manage model" Margin="5">
                <StackPanel>
                    <Button Content="SetDataPath" Width="200" Command="{Binding SetDataPathCommand}"/>
                    <TextBlock Text="{Binding DataPath}"/>
                    <Button Content="Retrain and Make Model" Width="200" Command="{Binding RetrainCommand}"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <ListView Grid.Row="2" ItemsSource="{Binding LogInstance.LogList}" Background="Transparent"/>
    </Grid>
</UserControl>
